import type { Metadata } from "next";
import { Inter } from "next/font/google";
import "./globals.css";

const inter = Inter({
  variable: "--font-inter",
  subsets: ["latin"],
  display: "swap",
});

export const metadata: Metadata = {
  title: "Wercor - Find The Right Agency. Faster.",
  description: "Agency Discovery Platform by Zaapr. Find verified agencies across 77+ countries worldwide.",
  keywords: "agency directory, digital agency, web design, marketing agency, find agency, wercor",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body className={`${inter.variable} antialiased`}>
        {children}
      </body>
    </html>
  );
}